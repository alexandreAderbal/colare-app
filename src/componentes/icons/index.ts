import Icon from 'react-native-vector-icons/MaterialIcons';
import { styled } from 'styled-components';

export const Search = styled(Icon).attrs(() => ({
    name: "search",
    size: 25,
}))``;

export const Menu = styled(Icon).attrs(() => ({
    name: "menu",
    size: 25,
}))``;

export const Home = styled(Icon).attrs(() => ({
    name: "home",
    size: 25,
}))`
`;
